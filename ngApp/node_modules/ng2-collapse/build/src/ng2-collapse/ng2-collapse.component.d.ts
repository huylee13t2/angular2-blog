import { EventEmitter, SimpleChange, OnChanges } from '@angular/core';
export declare class Ng2CollapseComponent implements OnChanges {
    collapsed: boolean;
    title: string;
    collapse: EventEmitter<any>;
    expand: EventEmitter<any>;
    ngOnChanges(changes: {
        [propName: string]: SimpleChange;
    }): void;
    toggle(): void;
    private hide();
    private show();
}
